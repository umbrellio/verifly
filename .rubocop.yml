require:
  - rubocop-rspec

AllCops:
  TargetRubyVersion: 2.3
  Exclude:
    - bin/*

Style/Documentation:
  # Using yard to check it
  Enabled: false

Metrics/BlockLength:
  Exclude:
    - spec/**/*_spec.rb

Lint/AmbiguousBlockAssociation:
  # REVIEW: why is that dangerous?
  Enabled: false

RSpec/MessageSpies:
  # Spies are not always usable, i.e. we want to stub something we haven't created
  EnforcedStyle: receive

RSpec/NestedGroups:
  # Extra nesting is good, not bad!
  Enabled: false
